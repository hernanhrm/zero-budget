# Fly.io app for the static SPA (nginx). Deploy from this directory (Docker build context = ./).
# Rename: fly apps create <name> then set `app` below, or fly launch.

app = "zero-budget-web"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

# Single Machine in the pool (no spare for HA). Use with min_machines_running = 0 for scale-to-zero.
# If an extra Machine already exists: fly scale count 1 -a zero-budget-web
[deploy]
  ha = false

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  # Scale to zero when idle: no Machines left running in the primary region (lower cost; cold starts on traffic).
  min_machines_running = 0

  [http_service.concurrency]
    type = "requests"
    soft_limit = 200
    hard_limit = 250

  [[http_service.checks]]
    grace_period = "60s"
    interval = "30s"
    method = "GET"
    timeout = "15s"
    path = "/"

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1
